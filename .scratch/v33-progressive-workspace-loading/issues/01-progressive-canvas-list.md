# 画布列表渐进加载

Type: task
Status: resolved

## 问题

画布列表等待全部任务、媒体和预览完成后才显示，并在存在生成中画布时每 4 秒重载整个列表。

## 验收

- 元数据先显示。
- 缩略图限流增量加载。
- 生成中状态只更新对应画布。

## Comments

- 已实现：列表元数据先渲染；预览使用鉴权 `/thumbnail?size=512`，4 路受控并发逐项回填；生成中只以 15 秒间隔刷新仍在生成的画布，不清空已显示缩略图。
- 回归：V33 渐进加载测试通过，Web UI 其余定向测试通过。
