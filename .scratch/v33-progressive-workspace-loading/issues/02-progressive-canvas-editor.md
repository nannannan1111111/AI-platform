# 画布编辑器首屏解耦

Type: task
Status: resolved

## 问题

编辑器等待配置、资产库、任务恢复和整份文档缩略图全部完成后才渲染画布。

## 验收

- 保存的节点和视口先渲染。
- 任务与媒体在后台增量恢复。
- 历史终态不执行无意义长退避，刚完成任务仍保留 V32 恢复能力。

## Comments

- 已实现：保存文档先返回并渲染；任务恢复、媒体缩略图和可用性检查后台执行，通过 `saas-canvas-hydrated` 合并；会话快照只保存稳定 JSON，避免失效 Blob URL。
- 本地未保存编辑期间不直接覆盖节点状态，结果缩略图按媒体 ID 合并。
- 回归：画布/媒体/SSE 定向测试及 V33 快照测试通过。
