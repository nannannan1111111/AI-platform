# 03 认证防滥用与登录风控

Type: task
Status: claimed
Stage: 公网安全
Blocked by: 01

## 目标

保护注册、登录和验证邮件接口，降低撞库、暴力破解、批量注册和邮件轰炸风险。

## 问题分析

- 当前认证端点没有服务端限流、失败退避或短时锁定。
- Web 有多个进程，单进程内存计数器会被绕过且重启后丢失。
- 只按 IP 限制会误伤 NAT 用户，只按邮箱限制又可被批量换邮箱绕过。
- 错误响应必须避免泄露邮箱是否注册，同时不能形成永久账号锁定 DoS。

## 设计方案

- 继续使用 PostgreSQL，不为限流单独引入 Redis。
- 新增短周期限流状态，按“可信客户端 IP + 规范化邮箱摘要 + 动作”联合计数。
- 建议初始策略：登录每 IP 10 次/10 分钟、每邮箱 5 次/10 分钟；注册每 IP 5 次/小时；验证邮件每账户 3 次/小时。阈值必须可配置并在预发布压测后确认。
- 登录失败采用有上限的短时退避；成功登录清理对应邮箱失败状态。
- 429 返回 `Retry-After`；登录失败保持统一文案。
- 限流记录只存最小摘要与时间，不保存明文密码、Token 或完整请求体。

## 实施步骤

### 1. 分析问题

- 为三个端点建立并发攻击测试，验证当前可无限请求。
- 明确信任代理后的客户端 IP 获取规则，与任务 04 共用实现。

### 2. 设计确认

- 评审表结构、原子更新 SQL、过期清理、隐私保留期和管理员解锁路径。
- 确认限流失败时数据库不可用的 fail-closed/fail-open 策略。

### 3. 修改代码

- 新增 Alembic 迁移、限流 Port/SQLAlchemy Adapter 和认证服务集成。
- 为注册、登录、邮件重发增加动作级策略和 `Retry-After`。
- 增加结构化安全事件日志和限流指标，不记录敏感字段。
- 更新运维文档和环境变量示例。

### 4. 质量检测

- 单元测试窗口边界、并发原子性、成功清理、IPv4/IPv6 和代理场景。
- PostgreSQL 多进程集成测试证明请求不能通过切换 Worker 绕过。
- 负载测试验证正常用户不会在目标并发下被误限流。

## 完成标准

- 三类认证端点都有跨进程限流。
- 撞库与邮件轰炸测试在阈值后稳定返回 429。
- 日志、数据库和响应均不泄露凭据或账户存在性。

## Comments

- 2026-08-17：用户决定仓库保持私有且不执行测试发布，并要求继续部署开发。任务 01 的源码门禁与真实 GitHub Actions 已全绿；平台侧强制分支保护因当前私有仓库套餐受限，但不阻塞本任务的认证安全实现。
- 2026-08-17（分析问题）：以当前 `create_app(InMemoryAccountAccess())` 连续发起 12 次未知邮箱错误登录，结果为 12 个 401、0 个 429，确认登录可无限尝试。代码审查同时确认注册、登录和验证邮件重发均无服务端限流；重复邮箱注册返回 409“邮箱已注册”，可被用于账户枚举；`Request.client.host` 与转发头之间也没有可信代理边界。
- 2026-08-17（设计确认）：采用 PostgreSQL 固定窗口和原子 UPSERT，使 4 个 Web Worker 共享计数，不新增 Redis。登录按 IP 10 次/10 分钟、邮箱摘要 5 次/10 分钟，注册按 IP 5 次/小时，验证邮件按账户 3 次/小时；阈值与窗口均由环境变量覆盖。标识使用部署密钥 HMAC-SHA-256 后落库，不保存明文邮箱、IP、密码或 Token；窗口结束后的记录在后续消费时清理。登录成功清理邮箱失败窗口，所有拒绝响应带 `Retry-After`。
- 2026-08-17（边界与失败语义）：默认只使用 TCP 直连地址。仅当直连来源属于 `TRUSTED_PROXY_CIDRS` 时，才从右向左剥离可信 `X-Forwarded-For` 代理链；畸形转发头整体忽略。限流数据库不可用时敏感认证入口 fail-closed 返回 503，避免故障期间形成无限撞库窗口。限流是短时窗口而非永久锁定；紧急解锁可删除目标摘要窗口，正常登录会自动清理邮箱维度。
- 2026-08-17（修改代码）：新增 `app.auth_abuse` Port、内存测试 Adapter、可信代理解析器与 PostgreSQL Adapter；迁移 `0060_auth_abuse_protection` 创建固定窗口表和到期索引。注册、登录、验证邮件重发已接入策略；重复邮箱、成功注册及注册邮件投递失败统一返回 202，不再通过响应暴露账户存在性。拒绝与后端故障输出可聚合的结构化安全事件，不记录邮箱、IP、密码、Token 或请求体。
- 2026-08-17（配置与运维）：生产启动强制要求至少 32 字节的 `AUTH_RATE_LIMIT_HASH_KEY`，Compose、`.env.example`、生产契约和运维文档已覆盖阈值、窗口、可信代理、503/429 语义、自动过期和紧急解锁 SQL。测试 HMAC Key 仅用于测试，未提交真实部署 Secret。
- 2026-08-17（本地质量检测）：Ruff 与严格 MyPy 通过；认证/运行时/容器定向测试 41 项通过，最终认证回归 16 项通过；临时 PostgreSQL 17 上 Alembic 完整升级→降级→升级及 20 并发跨两个 Adapter 的原子性测试 4 项通过（5 允许、15 拒绝）；完整后端 574 passed、1 个 Windows 权限测试跳过；前端 `npm ci/check/build`、Compose 解析与生产镜像构建通过。最终本地镜像 manifest list 为 `sha256:829e13cff13cb27691d26a015d7904d506a1d7e5abc36a95ea5cf1bcddc09482`，仅作本地门禁证据，未发布。

## Answer

认证防滥用实现已满足源码侧完成标准：三个敏感流程使用 PostgreSQL 跨进程限流，登录同时约束 IP 与规范化邮箱、成功后清理邮箱失败窗口；注册按 IP、验证邮件按账户约束。限流键只以 HMAC 摘要落库，429 携带 `Retry-After`，数据库故障时 fail-closed。注册响应不再区分邮箱是否已经存在。剩余确认项只有本提交的真实 GitHub Actions 结果；远端全绿后将补充运行链接并把工单状态改为 `resolved`。
