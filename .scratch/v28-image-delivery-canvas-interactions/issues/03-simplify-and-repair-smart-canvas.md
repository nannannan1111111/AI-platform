# 精简并修复智能画布交互

Type: task
Status: resolved

## Work

- 删除新建画布中的画布类型选择框和说明文字，创建行为继续固定为 smart。
- 保留 classic 历史数据与旧地址兼容。
- 修复弹窗 `stopPropagation` 与 document 级 CSP 事件委托冲突。
- 逐项真实浏览器验证生成、上传、节点、视图、编辑、下载、工作流和返回列表按钮。

## Comments

- 2026-08-24：浏览器已复现工作流面板关闭按钮事件无法到达 CSP bridge。

## Answer

- 删除了新建画布表单中的画布类型标签、只读“智能画布”框和说明文字；创建请求仍固定为 `kind: smart`。
- classic 历史数据、列表读取和旧地址兼容未删除，也未恢复 classic 新建入口。
- CSP bridge 从依赖 document 冒泡的委托改为静态白名单元素的受控直接绑定，解决弹窗父容器 `stopPropagation()` 导致关闭/工作流等按钮失效的问题。
- 真实浏览器验证通过：创建/打开、工作流打开/关闭/导入/导出、上传、节点添加/选择/拖动/Delete 删除、预览、裁剪、扩图、遮罩、画笔、宫格、下载、滚轮缩放、适应视图、快捷键、日志、资产库和返回列表；console 无 error/warn。
- 本地没有已发布模型和额度，未发起真实付费生图；生图按钮与 Ctrl+Enter 的提交/状态路径由 Web UI 自动化及后端生成链测试覆盖，生产未做写入验证。
