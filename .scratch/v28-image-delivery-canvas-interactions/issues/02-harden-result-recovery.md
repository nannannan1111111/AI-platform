# 加固 SSE、轮询与页面结果恢复

Type: task
Status: resolved

## Work

- 修复成功任务事件早于最终媒体事件时的客户端竞态。
- SSE 断开后使用有界退避轮询查询同一任务。
- 图片页和智能画布成功终态统一查询媒体并按媒体 ID 去重。
- 页面切换或重进时恢复已有任务，不重复提交或计费。

## Comments

- 2026-08-24：与 Worker 根因分别建立回归覆盖，避免以客户端轮询掩盖交付失败。

## Answer

- 图片页和智能画布不再把空媒体事件当作全部结果已到达，改用 `delivered_quantity` 与媒体查询完成收敛。
- SSE 断开后仅 GET 同一任务，使用 1.2 秒起步、最大 5 秒、最多 300 次的有界退避轮询；不会重新 POST 或创建 Provider 任务。
- 成功终态始终查询 `/media`，按媒体 ID、result reference 和 URL 去重；页面重进从持久任务/媒体恢复。
- 失败、取消、额度不足和超时继续使用服务端终态；未增加扣费、退款或额度流水写入路径。
- 生成链定向测试 `141 passed`，Web UI/SSE/恢复定向测试 `97 passed`。
