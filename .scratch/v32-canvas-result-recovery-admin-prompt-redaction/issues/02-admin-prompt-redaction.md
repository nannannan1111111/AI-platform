# 管理员任务明细提示词脱敏

Type: task
Status: claimed

## 问题

管理员活动/历史任务投影复用包含 `prompt` 的用户投影，前端也渲染实际提示词。

## 处理

管理员专用投影移除 `prompt` 字段，活动/历史两个管理员页面移除提示词展示；用户任务接口保持现有功能。

## Comments

- 不修改数据库中的历史提示词，只限制管理员任务明细 API/UI 的输出。
