Type: task
Status: resolved

# 画布媒体显示与裁剪分叉

定位并修复 SaaS 智能画布中持久媒体缩略图字段在二次加工路径丢失，以及裁剪覆盖源图片的问题；同步保持经典兼容入口行为一致。

## Acceptance

- 生成、上传、遮罩、宫格、裁剪媒体保留 `media_id` 与 `thumbnail`，保存/恢复后通过鉴权缩略图展示。
- 裁剪生成新图片节点，原图 URL、媒体 ID 和节点仍保留。
- 缩略图失败时保留既有原图交互兜底，不引入额外轮询。

## Comments

- 2026-08-25：基于 V28 生产候选分支开始最小范围修复。
- 2026-08-25：已完成缩略图字段保留、裁剪新节点分叉、资源版本更新；定向 Web UI 测试通过并部署到生产。
